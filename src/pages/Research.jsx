import React from "react";
import ReactApexChart from "react-apexcharts";

const Research = () => {
  return (
    <section className="p-6 space-y-6 mt-16">
      <div className="mb-8">
        <h1 className="text-2xl font-bold mb-2">Research Metrics</h1>
        <p className="text-gray-600">
          Detailed analysis of research performance and impact
        </p>
      </div>

      {/* Filters */}
      <div className="bg-white p-4 rounded-lg border border-gray-200 flex flex-wrap gap-4 items-center">
        {/* Add filter content */}
      </div>

      {/* Key Metrics */}
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        {/* Add metrics cards */}
      </div>

      {/* Research Impact Chart */}
      <div className="bg-white p-6 rounded-lg border border-gray-200">
        {/* Add chart content */}
      </div>

      {/* Publication Categories */}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
        {/* Add publication categories content */}
      </div>
    </section>
  );
};

export default Research;
